===============
CONF MONITOR API
===============

.. todo::
    Check if needed in documentation.
    If needed, code must be change because of os.environ['xxxx'] which failed
    when xxxx is not defined. xxxx is not always defined when building documenation.
    Should be replaced by os.environ.get('xxxx', 'default_value')

.. Comment
   .. automodapi:: conf_monitor
   :include-all-objects:

